services:
  - type: web
    name: flow-ai
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: RACK_ENV
        value: production
      - key: BRIDGETOWN_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
